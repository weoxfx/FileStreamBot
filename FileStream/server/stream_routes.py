# Legacy stream routes kept for compatibility — new routes are in api_routes.py
# This file is intentionally minimal; all active routes are in api_routes.py

from aiohttp import web

routes = web.RouteTableDef()
